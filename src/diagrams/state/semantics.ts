import type { CstNode, IToken } from 'chevrotain';
import type { ValidationError } from '../../core/types.js';

export function analyzeState(_cst: CstNode, _tokens: IToken[]): ValidationError[] {
  return [];
}

